services:
  - type: web
    name: valoraai-backend
    runtime: docker
    plan: free
    region: oregon
    healthCheckPath: /api/health
    envVars:
      - key: PORT
        value: 3000
      - key: ALLOWED_ORIGIN
        value: "*"
      - key: GEMINI_API_KEY
        sync: false
